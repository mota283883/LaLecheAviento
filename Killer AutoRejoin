local Players = game:GetService("Players")
local Lighting = game:GetService("Lighting")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")

local LocalPlayer = Players.LocalPlayer
local PlaceId = game.PlaceId
local JobId = game.JobId

if not whitelist[LocalPlayer.Name] then
    LocalPlayer:Kick("No autorizado")
    return
end

Lighting.ClockTime = 0
Lighting.Brightness = 1
Lighting.Ambient = Color3.fromRGB(0, 0, 0)
Lighting.OutdoorAmbient = Color3.fromRGB(0, 0, 0)
Lighting.FogColor = Color3.fromRGB(0, 0, 0)
Lighting.FogEnd = 1000
Lighting.FogStart = 0

local moon = Instance.new("Part")
moon.Name = "Luna"
moon.Shape = Enum.PartType.Ball
moon.Size = Vector3.new(30, 30, 30)
moon.Anchored = true
moon.CanCollide = false
moon.Material = Enum.Material.Neon
moon.Color = Color3.fromRGB(255, 255, 255)
moon.Position = Vector3.new(0, 1000, 0)
moon.Parent = workspace

local starEmitter = Instance.new("ParticleEmitter")
starEmitter.Texture = "rbxassetid://296874871"
starEmitter.Rate = 50
starEmitter.Lifetime = NumberRange.new(5, 10)
starEmitter.Size = NumberSequence.new(0.1)
starEmitter.Speed = NumberRange.new(0)
starEmitter.VelocitySpread = 360
starEmitter.LightEmission = 1
starEmitter.Parent = moon

local rainPart = Instance.new("Part")
rainPart.Name = "RainOrigin"
rainPart.Anchored = true
rainPart.CanCollide = false
rainPart.Transparency = 1
rainPart.Size = Vector3.new(500, 1, 500)
rainPart.Position = Vector3.new(0, 300, 0)
rainPart.Parent = workspace

local rain = Instance.new("ParticleEmitter")
rain.Texture = "rbxassetid://4842893125"
rain.Rate = 1500
rain.Lifetime = NumberRange.new(2, 3)
rain.Speed = NumberRange.new(60, 80)
rain.VelocitySpread = 10
rain.Rotation = NumberRange.new(0, 360)
rain.Transparency = NumberSequence.new(0.3)
rain.Size = NumberSequence.new(0.1)
rain.Acceleration = Vector3.new(0, -300, 0)
rain.LockedToPart = true
rain.Parent = rainPart

local rainSound = Instance.new("Sound")
rainSound.SoundId = "rbxassetid://1837746798"
rainSound.Looped = true
rainSound.Volume = 0.5
rainSound.Playing = true
rainSound.RollOffMode = Enum.RollOffMode.Linear
rainSound.EmitterSize = 150
rainSound.Parent = rainPart

task.spawn(function()
    while true do
        task.wait(math.random(8, 20))
        local flash = Instance.new("PointLight")
        flash.Color = Color3.new(1, 1, 1)
        flash.Range = 60
        flash.Brightness = 5
        local thunderPos = Vector3.new(math.random(-100, 100), 300, math.random(-100, 100))
        local thunderPart = Instance.new("Part")
        thunderPart.Size = Vector3.new(1, 1, 1)
        thunderPart.Anchored = true
        thunderPart.CanCollide = false
        thunderPart.Transparency = 1
        thunderPart.Position = thunderPos
        thunderPart.Parent = workspace
        flash.Parent = thunderPart
        task.delay(0.15, function() flash:Destroy() end)
        local thunderSound = Instance.new("Sound")
        thunderSound.SoundId = "rbxassetid://911882310"
        thunderSound.Volume = 1
        thunderSound.RollOffMode = Enum.RollOffMode.Linear
        thunderSound.EmitterSize = 100
        thunderSound.Parent = thunderPart
        thunderSound:Play()
        game.Debris:AddItem(thunderPart, 3)
    end
end)

local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "JxssInterface"
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true
ScreenGui.Parent = PlayerGui

local TimerLabel = Instance.new("TextLabel")
TimerLabel.Size = UDim2.new(0.3, 0, 0.05, 0)
TimerLabel.Position = UDim2.new(0.35, 0, 0.12, 0)
TimerLabel.BackgroundTransparency = 0.3
TimerLabel.BackgroundColor3 = Color3.fromRGB(10, 10, 20)
TimerLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
TimerLabel.Font = Enum.Font.GothamBlack
TimerLabel.TextScaled = true
TimerLabel.Text = "Server Hop en 90s"
TimerLabel.Parent = ScreenGui

local KillLabel = Instance.new("TextLabel")
KillLabel.Size = UDim2.new(0.3, 0, 0.05, 0)
KillLabel.Position = UDim2.new(0.35, 0, 0.18, 0)
KillLabel.BackgroundTransparency = 0.3
KillLabel.BackgroundColor3 = Color3.fromRGB(10, 10, 20)
KillLabel.TextColor3 = Color3.new(1, 1, 1)
KillLabel.Font = Enum.Font.GothamBold
KillLabel.TextScaled = true
KillLabel.Text = "Kills: ..."
KillLabel.Parent = ScreenGui

local MoonLabel = Instance.new("TextLabel")
MoonLabel.Size = UDim2.new(0.3, 0, 0.05, 0)
MoonLabel.Position = UDim2.new(0.35, 0, 0.24, 0)
MoonLabel.BackgroundTransparency = 0.3
MoonLabel.BackgroundColor3 = Color3.fromRGB(10, 10, 20)
MoonLabel.TextColor3 = Color3.new(1, 1, 1)
MoonLabel.Font = Enum.Font.GothamBlack
MoonLabel.TextScaled = true
MoonLabel.Text = "JxssMoon Kill"
MoonLabel.Parent = ScreenGui

local function rainbowify(label)
    task.spawn(function()
        while true do
            local t = os.clock() * 2
            local r = math.sin(t) * 127 + 128
            local g = math.sin(t + 2) * 127 + 128
            local b = math.sin(t + 4) * 127 + 128
            label.TextColor3 = Color3.fromRGB(r, g, b)
            task.wait(0.05)
        end
    end)
end

rainbowify(TimerLabel)
rainbowify(KillLabel)
rainbowify(MoonLabel)

task.spawn(function()
    local leaderstats = LocalPlayer:WaitForChild("leaderstats", 10)
    if leaderstats then
        local killStat = leaderstats:FindFirstChild("Kills") or leaderstats:FindFirstChild("kills")
        if killStat then
            KillLabel.Text = "Kills: " .. tostring(killStat.Value)
            killStat:GetPropertyChangedSignal("Value"):Connect(function()
                KillLabel.Text = "Kills: " .. tostring(killStat.Value)
            end)
        end
    end
end)

local timeLeft = 90
task.spawn(function()
    while true do
        TimerLabel.Text = "Server Hop en " .. timeLeft .. "s"
        timeLeft -= 1
        task.wait(1)
    end
end)

local function ServerHop()
    local servers = {}
    local requestFunction = syn and syn.request or http_request or request
    if not requestFunction then return end
    local success, response = pcall(function()
        return requestFunction({
            Url = "https://games.roblox.com/v1/games/" .. PlaceId .. "/servers/Public?sortOrder=Desc&limit=100"
        })
    end)
    if not success or not response or not response.Body then return end
    local data = HttpService:JSONDecode(response.Body)
    for _, server in ipairs(data.data) do
        if server.id ~= JobId and server.playing < server.maxPlayers then
            table.insert(servers, server.id)
        end
    end
    if #servers > 0 then
        TeleportService:TeleportToPlaceInstance(PlaceId, servers[math.random(1, #servers)])
    end
end

local function MonitorDeath()
    local function onCharacter(character)
        local humanoid = character:WaitForChild("Humanoid", 5)
        if humanoid then
            humanoid.Died:Connect(ServerHop)
        end
    end
    if LocalPlayer.Character then onCharacter(LocalPlayer.Character) end
    LocalPlayer.CharacterAdded:Connect(onCharacter)
end

MonitorDeath()

task.spawn(function()
    while true do
        task.wait(90)
        timeLeft = 90
        pcall(ServerHop)
    end
end)

getgenv().AutoPunchEnabled = true
local fastHitActive = true

local function equipPunchHand()
    local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    for _, tool in ipairs(LocalPlayer.Backpack:GetChildren()) do
        if tool.Name:lower():find("punch") then
            tool.Parent = char
        end
    end
end

local function getHand()
    local char = LocalPlayer.Character
    if not char then return nil end
    return char:FindFirstChild("RightHand") or char:FindFirstChild("Right Arm")
end

local function autoPunch()
    while fastHitActive do
        local char = LocalPlayer.Character
        if char and char:FindFirstChild("Humanoid") then
            local punch = LocalPlayer.Backpack:FindFirstChild("Punch")
            if punch then
                punch.Parent = char
                if punch:FindFirstChild("attackTime") then
                    punch.attackTime.Value = 0
                end
            end
        end
        task.wait(0.05)
    end
end

task.spawn(autoPunch)

RunService.Heartbeat:Connect(function()
    if not getgenv().AutoPunchEnabled then return end
    local char = LocalPlayer.Character
    if not char then return end

    if not char:FindFirstChildOfClass("Tool") then
        equipPunchHand()
    end

    local hand = getHand()
    if hand then
        for _, target in pairs(Players:GetPlayers()) do
            if target ~= LocalPlayer and target.Character then
                local root = target.Character:FindFirstChild("HumanoidRootPart")
                if root and root:IsDescendantOf(Workspace) then
                    spawn(function()
                        for i = 1, 10 do
                            firetouchinterest(hand, root, 0)
                            firetouchinterest(hand, root, 1)
                            task.wait(0.005)
                        end
                    end)
                end
            end
        end
    end

    pcall(function()
        local tool = char:FindFirstChildOfClass("Tool")
        if tool then
            tool:Activate()
        else
            equipPunchHand()
        end
    end)
end)
